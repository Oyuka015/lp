{
  "name": "foodrush-app",
  "version": "1.0.0",
  "description": "Food delivery app with optimized performance",
  "scripts": {
    "minify": "node scripts/minify.js",
    "build": "npm run minify"
  },
  "devDependencies": {
    "terser": "^5.24.0"
  }
}

